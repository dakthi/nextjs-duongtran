"use client";
import React from "react";

/**
 * PopupWidget component - Currently disabled
 * This component was removed as requested by the user
 *
 * The popup widget functionality can be re-implemented if needed in the future
 * using a simpler approach without Headless UI dependencies.
 */
export function PopupWidget() {
  return null;
}
